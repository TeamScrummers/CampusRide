import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: '@beyonk/svelte-mapbox',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
